#include <stdio.h>

#include "../../basic.h"
#include "../my_math.h"


int main(void)
{
    f32 x = 3.03;
    f32 y = 3.0343;
    f32 z = 0.01;

    if (x ==y)
        printf("yah");

    /*
    printf("x = %f\n", abs(x));
    printf("y = %f\n", abs(y));
    printf("z = %f\n", abs(z));
    */
}
