# PLANNED(feat/miniaudio-music-system)

After approval, vendor the official single-header library here as:

- `external/miniaudio.h`

Source: https://github.com/mackron/miniaudio (release/miniaudio.h)

No extra linker flags required for default backends on Windows/Linux when used as header-only with `MINIAUDIO_IMPLEMENTATION` in one translation unit.
